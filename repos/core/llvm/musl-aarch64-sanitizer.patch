--- llvm-project-21.1.8.src.orig/compiler-rt/lib/sanitizer_common/sanitizer_platform_limits_linux.cpp
+++ llvm-project-21.1.8.src/compiler-rt/lib/sanitizer_common/sanitizer_platform_limits_linux.cpp
@@ -64,7 +64,9 @@
 COMPILER_CHECK(struct___old_kernel_stat_sz == sizeof(struct __old_kernel_stat));
 #endif
 
+#if !SANITIZER_MUSL
 COMPILER_CHECK(struct_kernel_stat_sz == sizeof(struct stat));
+#endif
 
 #if defined(__i386__)
 COMPILER_CHECK(struct_kernel_stat64_sz == sizeof(struct stat64));
--- llvm-project-21.1.8.src.orig/compiler-rt/lib/sanitizer_common/sanitizer_platform_limits_posix.cpp
+++ llvm-project-21.1.8.src/compiler-rt/lib/sanitizer_common/sanitizer_platform_limits_posix.cpp
@@ -381,8 +381,13 @@
   unsigned struct_user_regs_struct_sz = sizeof(struct user_regs_struct);
   unsigned struct_user_fpregs_struct_sz = sizeof(struct __riscv_q_ext_state);
 #elif defined(__aarch64__)
+#if SANITIZER_MUSL
+  unsigned struct_user_regs_struct_sz = sizeof(struct user_regs_struct);
+  unsigned struct_user_fpregs_struct_sz = sizeof(struct user_fpsimd_struct);
+#else
   unsigned struct_user_regs_struct_sz = sizeof(struct user_pt_regs);
   unsigned struct_user_fpregs_struct_sz = sizeof(struct user_fpsimd_state);
+#endif
 #elif defined(__loongarch__)
   unsigned struct_user_regs_struct_sz = sizeof(struct user_pt_regs);
   unsigned struct_user_fpregs_struct_sz = sizeof(struct user_fp_state);
--- llvm-project-21.1.8.src.orig/compiler-rt/lib/sanitizer_common/sanitizer_stoptheworld_linux_libcdep.cpp
+++ llvm-project-21.1.8.src/compiler-rt/lib/sanitizer_common/sanitizer_stoptheworld_linux_libcdep.cpp
@@ -517,7 +517,11 @@
 #    define REG_SP regs[EF_R29]
 
 #elif defined(__aarch64__)
+#if SANITIZER_MUSL
+typedef struct user_regs_struct regs_struct;
+#else
 typedef struct user_pt_regs regs_struct;
+#endif
 #define REG_SP sp
 static constexpr uptr kExtraRegs[] = {0};
 #define ARCH_IOVEC_FOR_GETREGSET
