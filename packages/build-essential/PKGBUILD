pkgname=build-essential
pkgver=0.3
pkgrel=1
pkgdesc='A meta-package which pulls in essential build tools'
arch=(x86_64)
url=''
license=(BSD)
groups=(base)
depends=(
    binutils
    busybox
    fakeroot
    file
    gcc
    linux-headers
    m4
    make
    musl-dev
    pacman-build
    patch
    sudo
    upx
)
makedepends=()
options=()
changelog=ChangeLog
source=(
)
sha256sums=(
)

package() {
    /bin/true
}
